import { WebhookEvent } from '../types';
export interface WebhookVerifyOptions {
    payload: string | Buffer;
    signature: string;
    secret: string;
    tolerance?: number;
}
export interface WebhookParseOptions {
    body: string;
    signature: string;
    secret: string;
}
/**
 * Verify a SipHeron webhook signature exactly.
 * Uses constant-time equality to prevent timing attacks.
 */
export declare function verifyWebhookSignature({ payload, signature, secret, tolerance }: WebhookVerifyOptions): boolean;
/**
 * Parses and verifies a webhook payload in one step.
 */
export declare function parseWebhookEvent({ body, signature, secret }: WebhookParseOptions): WebhookEvent;
//# sourceMappingURL=verify.d.ts.map