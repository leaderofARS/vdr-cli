"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.verifyWebhookSignature = verifyWebhookSignature;
exports.parseWebhookEvent = parseWebhookEvent;
const crypto_1 = require("crypto");
const errors_1 = require("../errors");
/**
 * Verify a SipHeron webhook signature exactly.
 * Uses constant-time equality to prevent timing attacks.
 */
function verifyWebhookSignature({ payload, signature, secret, tolerance = 300 // 5 minutes default tolerance
 }) {
    if (!payload || !signature || !secret) {
        return false;
    }
    try {
        const payloadBuffer = Buffer.isBuffer(payload) ? payload : Buffer.from(payload, 'utf-8');
        const computedSignature = (0, crypto_1.createHmac)('sha256', secret)
            .update(payloadBuffer)
            .digest('hex');
        // Optional: if signatures in SipHeron use timestamps (e.g. `t=123,v1=abc`),
        // you would parse that here. For now, assuming direct HMAC hex.
        const sigBuffer = Buffer.from(signature, 'utf8');
        const computedBuffer = Buffer.from(computedSignature, 'utf8');
        if (sigBuffer.length !== computedBuffer.length) {
            return false;
        }
        return (0, crypto_1.timingSafeEqual)(sigBuffer, computedBuffer);
    }
    catch (err) {
        return false;
    }
}
/**
 * Parses and verifies a webhook payload in one step.
 */
function parseWebhookEvent({ body, signature, secret }) {
    const isValid = verifyWebhookSignature({
        payload: body,
        signature,
        secret
    });
    if (!isValid) {
        throw new errors_1.SipHeronError('Invalid webhook signature', 'WEBHOOK_SIGNATURE_INVALID', 401);
    }
    try {
        return JSON.parse(body);
    }
    catch (err) {
        throw new errors_1.ValidationError('Webhook payload is not valid JSON');
    }
}
//# sourceMappingURL=verify.js.map