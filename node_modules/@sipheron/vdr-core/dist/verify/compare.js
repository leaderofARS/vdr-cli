"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.compareHashes = compareHashes;
exports.verifyBuffer = verifyBuffer;
const hash_1 = require("../hash");
/**
 * Compare two SHA-256 hashes for equality.
 * Uses constant-time comparison to prevent timing attacks.
 * Both hashes are normalized to lowercase before comparison.
 *
 * @param hashA - First hash
 * @param hashB - Second hash
 * @returns true if hashes are identical
 */
function compareHashes(hashA, hashB) {
    return (0, hash_1.constantTimeCompare)((0, hash_1.normalizeHash)(hashA), (0, hash_1.normalizeHash)(hashB));
}
/**
 * Verify a document Buffer matches a known hash.
 * Hashing happens locally — document never transmitted.
 */
async function verifyBuffer(file, expectedHash) {
    const { hashDocument } = await Promise.resolve().then(() => __importStar(require('../hash')));
    const computed = await hashDocument(file);
    return compareHashes(computed, expectedHash);
}
//# sourceMappingURL=compare.js.map