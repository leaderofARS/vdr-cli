/**
 * @sipheron/vdr-core
 *
 * Core document verification library for SipHeron VDR.
 * Anchor any document's SHA-256 fingerprint permanently to Solana.
 * Verify authenticity anywhere, forever.
 *
 * @example
 * import { SipHeron } from '@sipheron/vdr-core'
 *
 * const sipheron = new SipHeron({ apiKey: 'your-key', network: 'devnet' })
 *
 * const anchor = await sipheron.anchor({ file, name: 'Contract v1' })
 * console.log(anchor.verificationUrl)
 *
 * const result = await sipheron.verify({ file })
 * console.log(result.authentic) // true
 */
export { SipHeron } from './client';
export { hashDocument, hashFile, hashStream, hashBase64, isValidHash, normalizeHash } from './hash';
export { verifyHashStandalone as verifyHash } from './verify';
export { verifyLocally, verifyOnChain, deriveAnchorAddress } from './verify';
export { verifyWebhookSignature, parseWebhookEvent } from './webhook';
export { generateCertificateId, buildCertificateUrl, prepareCertificateData, getCertificateProofText, } from './certificate';
export { getExplorerUrl, isValidTxSignature, estimateAnchorCost, SIPHERON_CONTRACT, SIPHERON_PROGRAM_ID, ANCHOR_SEED, PROTOCOL_VERSION, MAX_BATCH_SIZE, HASH_LENGTH, CONFIRMATION_DEPTH, SOLANA_NETWORKS, anchorToSolana, } from './anchor';
export type { SipHeronConfig, NetworkType, AnchorOptions, AnchorResult, AnchorStatus, BatchAnchorOptions, BatchAnchorResult, DirectAnchorOptions, DirectAnchorResult, VerifyOptions, VerificationResult, VerificationStatus, OnChainVerificationOptions, OnChainVerificationResult, WebhookEventType, WebhookEvent, AnchorCreatedEvent, AnchorConfirmedEvent, AnchorFailedEvent, VerificationPerformedEvent, QuotaWarningEvent, AnomalyDetectedEvent, } from './types';
export type { CertificateData } from './certificate';
export { SipHeronError, AuthenticationError, AnchorNotFoundError, HashMismatchError, NetworkError, RateLimitError, ValidationError, AnchorRevokedError, QuotaExceededError, SolanaConnectionError, TransactionError } from './errors';
//# sourceMappingURL=index.d.ts.map